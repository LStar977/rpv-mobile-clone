// Preview route for the redesign Proposal Detail + vote (screen 06 / 08).
// Reachable via the redesign Feed. Remove at IA cutover.
import { ProposalDetailScreen } from '../components/redesign/screens/ProposalDetailScreen';

export default function RedesignProposalRoute() {
  return <ProposalDetailScreen />;
}
